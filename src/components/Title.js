import React from 'react';

const titleStyle = {
  fontFamily: 'tahoma',
  fontSize: '24px',
  textAlign: 'center'
};

const Title = () => (
  <div style={titleStyle}>
    Book Search
  </div>
);

export default Title;