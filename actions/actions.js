export const setTopic = (topic) => {
  return {
    type: 'SET_TOPIC',
    topic
  };
};

export const setDisplayMode = (displayMode) => {
  return {
    type: 'SET_DISPLAY_MODE',
    displayMode
  };
};